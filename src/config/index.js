const config = {
  apiServerUrl: import.meta.env.VITE_API_SERVER_URL || '',
  mockDocUrl: import.meta.env.VITE_MOCK_DOC_URL || 'https://example.com/docs/mock_birth_certificate.pdf',
};

export default config;
